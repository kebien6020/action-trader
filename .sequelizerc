const { resolve } = require('path')

module.exports = {
  'migrations-path': resolve('server/db/migrations'),
  'models-path': resolve('server/db/models'),
  'config': resolve('server/db/config.json'),
}
