// import request from 'supertest'
// import {
//   actions,
//   // subscriptions,
//   // testPush,
//   // config,
// } from './routes'

const routes = require('./routes')

// import * as actionHandlers from './actions'

// const request = () => {}

describe('routes mapping', () => {
  it('maps GET /actions/ to actionsHandlers.list', () => {
    // actionHandlers.list = jest.fn()
    // return request(actions).get('/')
    //   .then(() => {
    //     expect(actionHandlers.list).toHaveBeenCalledTimes(1)
    //   })
  })
})
